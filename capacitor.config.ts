import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
	appId: 'com.zsmartex.huuhait',
	appName: 'ZSmartex',
	webDir: 'dist',
	bundledWebRuntime: false,
}

export default config
